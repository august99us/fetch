{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "full",
  "description": "Capability for the full app window",
  "windows": ["full"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-close",
    "dialog:allow-open"
  ]
}